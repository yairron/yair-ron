# הוראות לעבודה בפרויקט

## עדכוני ביטוח לאומי (NII)

אחרי כל שינוי ב-`BTL/senior_rights/data/nii-constants.json` — להריץ:

```
python BTL/support_files/senior_rights/scripts/update_nii_values.py
```

הסקריפט מעדכן ערכי fallback ב-`<span data-nii="...">` בכל קבצי ה-HTML תחת `BTL/senior_rights/`.

**חשוב:** הסקריפט הזה מזהה רק את התבנית הפשוטה `<span data-nii="KEY">` (בלי תכונות נוספות). הוא **לא** מעדכן:
- עמודים עם תוכן סטטי שהוטמע בשיטת "מנוע הליבה" (ראו הסעיף הבא) — 7 עמודים, ראו רשימה שם.
- עמודים מקבוצה ד׳ (ראו הסעיף שאחריו) — כולל גם מופעי `data-nii` עם תכונת `data-format` נלווית.

**לכן, אחרי כל עדכון של `nii-constants.json`, יש להריץ את כל שלושת התהליכים הבאים** (לא רק את זה): את הסקריפט הזה, את `bake_static_content.py` על כל עמודי "מנוע הליבה", ואת `sync_static_values.py` על כל עמודי קבוצה ד׳.

**קובצי ריכוז לכלי בינה מלאכותית:**
```
python BTL/support_files/senior_rights/scripts/build_ai_summary.py
```
בונה מחדש את `BTL/ai-summary.html` (אינדקס קצר בלבד) ואת קובצי התוכן המלא לפי נושא תחת `BTL/ai-content/*.html`. **עודכן 20.07.2026:** עד לתאריך זה זה היה קובץ ריכוז מאוחד יחיד (~800KB) — פוצל אחרי שג'מיני דיווח בפועל על חסימה בשליפתו (כלי fetch/grounding חיצוניים מגבילים גודל תוכן שהם שולפים מכתובת בודדת). כל עמוד שמסומן כ-`"include"` בתוך `KNOWN_FILES` בסקריפט חייב להיות משובץ גם לנושא אחד בדיוק בתוך `TOPICS` (מילון נפרד באותו סקריפט) — הסקריפט מדפיס אזהרה נפרדת אם עמוד `include` לא משובץ לאף נושא. `ai-summary.html` עצמו הוא עכשיו רשימה קצרה (כמה KB בלבד): לכל נושא — כתובת מלאה לקובץ התוכן שלו (`https://yairron.com/btl/ai-content/<slug>.html`), כותרת, תיאור, ורשימת העמודים שבתוכו. הסקריפט סורק בפועל את `BTL/senior_rights`, `BTL/new_immigrants`, ו-`BTL/additional_guides/html`, ומדפיס אזהרה מפורשת אם נמצא קובץ חדש שלא מסווג ב-`KNOWN_FILES`, או קובץ ברשימה שנמחק מהדיסק — **יש להריץ אותו ולבדוק את הפלט בכל פעם שנוסף/מוסר עמוד**, לא רק אחרי עדכון `nii-constants.json`.

**עודכן 22.07.2026 - `.txt` הוחלף ב-HTML מינימלי:** ג'מיני דיווח בפועל שכלי הגלישה שלו לא ניגש בכלל לקבצי `.txt` (רק ל-HTML) — כנראה כלי "גלישה" מבוססי-רינדור לא יודעים לטפל בתגובת `text/plain`. הפתרון: `wrap_html()` בתוך הסקריפט עוטף את אותו טקסט בדיוק במעטפת HTML מינימלית (`<pre>` בלי CSS/JS/תפריטים) כדי שה-Content-Type יהיה `text/html` — עדיין קל בהרבה מעמודי המדריך המלאים. **אין יותר קובצי `.txt` בהקשר הזה באתר בכלל.**

**הוסר ב-22.07.2026:** `BTL/files/BTL_CLEANED` (תוצר חד-פעמי וידני מסקריפט `gemini-code-*.py` בשורש הריפו, מלפני ש-`build_ai_summary.py` נבנה - לא מתעדכן אוטומטית, לא מקושר מהאתר, ולא חסום ב-robots.txt בניגוד ל-`support_files`) נמחק יחד עם הסקריפט שיצר אותו - תוכן מיושן (נתוני ינואר 2025) שהוחלף לחלוטין ע"י התהליך המתועד למעלה.

**חשוב — `netlify/functions/btl-chat-claude-ai.js` (הצ'אט הפנימי של האתר, ראו הסעיף הבא) תלוי ברשימת קובצי הנושא ולא בפירוק האינדקס:** הפונקציה מחזיקה `CONTENT_FILES` — רשימת שמות הקבצים הקבועה של כל קובצי `ai-content/*.html` — קוראת את כולם **מהדיסק המקומי** (`fs.readFileSync`, ראו הסעיף הבא לפירוט למה לא `fetch`), מחלצת את הטקסט הגולמי מתוך ה-`<pre>` של כל אחד (`extractPreText()`, כולל ביטול ה-escaping ל-`& < >`), ומאחדת בחזרה לטקסט אחד לפני הפירוק לסעיפים. **אם משנים שם/מוסיפים/מסירים נושא ב-`TOPICS` בתוך `build_ai_summary.py`, יש לעדכן ידנית גם את `CONTENT_FILES` בתוך `btl-chat-claude-ai.js`** — אין קישור אוטומטי בין השניים.

**קישורי `<link rel="alternate">` לכלי AI חיצוניים - נוסף 22.07.2026:**
```
python BTL/support_files/senior_rights/scripts/add_ai_alternate_links.py
```
**למה בכלל צריך את זה, מעבר לפיצול ל-`ai-content`:** פרפלקסיטי וג'מיני דיווחו שהם "מוצאים את האתר ומבינים את הנושא הכללי" אבל לא מצליחים לשלוף פרטים ספציפיים - כי כשהם שולפים עמוד מדריך בזמן אמת הם מורידים את קובץ ה-HTML הגולמי במלואו (כולל כל קוד העיצוב/JS - עד 300KB+ בעמודים כמו `faq.html`), לא רק את הטקסט. המכסה שלהם לתוכן מעמוד בודד נגמרת על קוד העיצוב לפני שהם מגיעים לפרטים בעומק העמוד. `ai-content/*.html` (הסעיף הקודם) כבר פותר את זה - אבל שום דבר בעמודי ה-HTML עצמם לא הצביע להם שהמשאב הזה קיים.

הסקריפט מוסיף/מעדכן לכל עמוד המשובץ ב-`TOPICS` (ראו הסעיף הקודם) תג `<link rel="alternate" type="text/html" href="https://yairron.com/btl/ai-content/<slug>.html">` מיד אחרי ה-`meta description` ב-`<head>` - תקן רשת סטנדרטי (אותו מנגנון כמו גילוי RSS) שמצביע על גרסה קלה בהרבה של אותו עמוד, בלי לשנות שום דבר בתצוגה לבן אדם. **אידמפוטנטי ומעדכן-בעצמו** - מזהה כל תג `rel=alternate` קיים שמצביע ל-`ai-content` (גם תגים מדור קודם עם `type` ישן) ומחליף אותו בתג הנכון העדכני. **יש להריץ אותו אחרי כל הרצה של `build_ai_summary.py` שמוסיפה/מסירה נושא או עמוד**, כדי שהקישורים יישארו מסונכרנים עם `TOPICS`.

**חשוב — הסתייגות אמיתית שהתגלתה בבדיקה בפועל (22.07.2026):** פרפלקסיטי דיווח במפורש שהוא **לא עוקב אחרי `rel="alternate"` בכלל** - הכלי שלו ניגש רק לכתובות שמופיעות כ-`<a href>` רגיל וגלוי בעמוד (או בתוצאת חיפוש), ובדק בפועל את דף הבית ואת `additional_guides_index.html` ולא מצא שם קישור רגיל ל-`ai-content`. **הפתרון שהתווסף בעקבות זה:** קישור אמיתי וגלוי (אך צנוע, בפוטר) ל-`ai-summary.html` — ב-`hamburger-nav.js` (מופיע אוטומטית בכל עמודי `senior_rights`/`new_immigrants`/`index.html` שטוענים את הסקריפט המשותף) ובפוטר של `additional_guides_index.html`. כלומר `rel="alternate"` לא מספיק לבדו - חובה גם קישור `<a href>` אמיתי ממקום נגיש בניווט הרגיל.

**קובץ מפת אתר:**
```
python BTL/support_files/senior_rights/scripts/build_sitemap.py
```
בונה מחדש את `BTL/sitemap.xml` עם כתובת מלאה לכל עמוד HTML תחת `BTL/senior_rights`, `BTL/new_immigrants`, ו-`BTL/additional_guides/html` (כולל עמודים שלא ב-`ai-summary.html` — מפת האתר היא לגילוי כללי, לא רק לתוכן שאומת), את `BTL/ai-summary.html` (האינדקס) וכל קובצי `BTL/ai-content/*.html` (התוכן לפי נושא) — נוספים במפורש ב-`discover_public_pages()`, כי הם לא תחת אחת מתיקיות התוכן הרגילות ולא נתפסים ע"י הלולאה הרגילה, אבל כן משאבים ציבוריים אמיתיים (האינדקס מקושר גם מ-`robots.txt`). **יש להריץ מחדש בכל פעם שנוסף/מוסר עמוד או נושא.**

**חשוב — תוקן ב-12.07.2026 אחרי אימות ישיר מול השרת החי (curl, לא הנחה):** בניגוד למה שהיה מתועד כאן קודם, `https://yairron.com` **אינו** שורש הפרסום של `BTL` ישירות. הכתובת האמיתית דורשת תחילית `/btl/` (אותיות קטנות דווקא — יש הפניית 301 מ-`/BTL/` עם אותיות גדולות, וזו כוללת גם הורדת סיומת `.html` באותה קפיצה). `build_sitemap.py` כבר בנוי לפי זה: `https://yairron.com/btl/<נתיב>` בלי `.html`, ועמוד הבית עצמו הוא `https://yairron.com/btl/` (לא `btl/index`). כל כתובת בקובץ `sitemap.xml` הנוכחי אומתה בפועל (200) מול השרת.

**תוקן שוב 27.07.2026 — הפעם אחרי דיווח אמיתי מ-Google Search Console:** ההנחה שהייתה כאן (שכתובת עם `.html` בתחילית התקנית `btl/` עוברת הפניה לכתובת בלי הסיומת) **הייתה שגויה**. אומת מחדש ישירות מול השרת (curl): כתובת כמו `https://yairron.com/btl/senior_rights/nursing_home_guide.html` **לא** עוברת שום הפניה — היא חיה במקביל לכתובת הקנונית בלי הסיומת, שתיהן מחזירות 200 עם תוכן זהה. Search Console דיווח על כך בפועל כ-"Duplicate without user-selected canonical" בשני עמודים (`senior_rights/holocaust_survivors_rights.html`, `senior_rights/nursing_home_guide.html`) — אבל הבעיה קיימת פוטנציאלית בכל עמוד באתר, לא רק בשניים שכבר נסרקו במקרה דרך הכתובת עם הסיומת. **הפתרון: תג קנוני בכל עמוד**, ראו הסעיף הבא.

**תג `<link rel="canonical">`, נוסף 27.07.2026:**
```
python BTL/support_files/senior_rights/scripts/add_canonical_links.py
```
מוסיף/מעדכן ב-`<head>` של כל עמוד ציבורי (לפי אותה רשימת `discover_public_pages()` שמזינה את `build_sitemap.py`) תג `<link rel="canonical" href="...">` שמצביע על הכתובת הקנונית שלו (`to_canonical_url()` — אותה פונקציה בדיוק שבונה את הכתובות ב-`sitemap.xml`, אין כפל-מקור). אידמפוטנטי ומעדכן-בעצמו כמו `add_ai_alternate_links.py`. **יש להריץ מחדש בכל פעם שנוסף עמוד ציבורי חדש** (כלול כעת גם בסעיף "כל עמוד חדש, מכל סוג" למטה).

**`netlify.toml` — קובץ יחיד ופעיל, בשורש הריפו בלבד.** Netlify קורא רק את קובץ ה-`netlify.toml` שיושב בשורש אתר הפרסום (base directory) — לא כל קובץ בשם הזה שנמצא בתוך תיקיות המשנה. **נמחקו 27.07.2026** שני קבצי `netlify.toml` יתומים שהיו קיימים בפועל בתוך `BTL/netlify.toml` ו-`BTL/senior_rights/netlify.toml` — שרידי סקאפולד ישן (עם כלל `[[redirects]] from="/*" to="/index.html" status=200`, טיפוסי לתבנית SPA) שמעולם לא היו אמורים להיות פעילים ולא היו, כי הם לא בשורש הריפו. לא היה להם השפעה בפועל על האתר החי, אבל בלבלו כשחיפשנו כללי redirect בעת חקירת בעיית ה-"Redirect error" ב-Search Console (ראו הבא). אם צריך כלל redirect/header נוסף — הוא שייך אך ורק בקובץ `netlify.toml` בשורש, או בקובץ `_redirects` (גם הוא בשורש, ראו למטה).

**`_redirects` (בשורש) — תוקן 27.07.2026 בעקבות דיווח אמיתי מ-Google Search Console על "Redirect error":** הכתובת שדווחה, `https://yairron.com/btl/additional_guides/html/index`, עברה שרשרת מעורבת של הפניית 301 אמיתית של Netlify (מ-`.../index` ל-`.../` עם `/` בסוף, מנגנון פנימי של Netlify) ואז הפניית `meta refresh` נוספת בצד הלקוח מתוך תוכן `index.html` עצמו (ראו הסבר על העמוד הזה בסעיף "תיקיית BTL/additional_guides" למטה) — צירוף שכנראה בלבל את הזחילה של גוגל. **הפתרון:** שלוש שורות `301` מפורשות ב-`_redirects` (`/btl/additional_guides/html/index`, `.../index.html`, ו-`.../` עם `/` בסוף — כל הצורות שאפשר להגיע אליהן) שמפנות ישירות ובקפיצה יחידה לכתובת הקנונית הסופית `.../additional_guides_index`, בלי לעבור דרך `index.html` ותוכנו בכלל. קובץ `index.html` עצמו נשאר קיים בדיסק כגיבוי סביר (אם `_redirects` איכשהו לא נטען) אבל בפועל כללי ה-`_redirects` נבדקים לפני הגשת קבצים סטטיים ואמורים ליירט את הבקשה קודם.

`/robots.txt` — קובץ קבוע בשורש הריפו (לא בתוך `BTL/`!) — לא סקריפט, לא צפוי להשתנות לעיתים קרובות. **חובה שישב בשורש הריפו דווקא**, כי זוחלים בודקים רק את `domain.com/robots.txt` בשורש האמיתי של הדומיין — קובץ בתוך `BTL/robots.txt` (שם היה יושב במקור) לעולם לא היה מתגלה, גם אם תוכנו נכון. מתיר סריקה כללית (גם ל-`User-agent: *` וגם בבלוק נפרד וזהה ל-`User-agent: Google-Extended`, כדי לאשר גישה במפורש לכלי ה-AI הגנרטיביים של גוגל, כולל ג'מיני), חוסם את `/yr1/`, `/mda/`, ו-`/btl/support_files/`, ומפנה למפת האתר (`https://yairron.com/btl/sitemap.xml`) ולקובץ הריכוז (`https://yairron.com/btl/ai-summary.html`).

**חריגים מפורשים בתוך `/btl/support_files/` — עודכן 20.07.2026:** למרות שהתיקייה חסומה באופן כללי (מכילה סקריפטים פנימיים וארכיון קבצים ישנים שהוצאו במפורש מתצוגת האתר הפעיל, ראו `BTL/support_files/moved_files.md`), שני עמודים חיים ומקושרים בפועל (`senior_rights/Information_Sources.html` ו-`senior_rights/benefit-combinations.html`) שולפים בזמן ריצה (fetch) קבצים מתת-התיקיות `support_files/files/` ו-`support_files/json/` בהתאמה. בעקבות דיווח בפועל של ג'מיני על חסימה, נוספו שורות `Allow:` ממוקדות לשתי תת-התיקיות האלה בלבד (בשני הבלוקים, `*` ו-`Google-Extended`) — **לא** נפתחה גישה לכל `support_files` (שם עדיין יושבים הסקריפטים והארכיון הישן, במכוון לא ציבוריים). אם מתווסף עמוד חי נוסף שתלוי בשליפת קובץ מתת-תיקייה אחרת תחת `support_files`, יש להוסיף שורת `Allow:` ממוקדת דומה, לא לפתוח את כל התיקייה.

## עוזר AI (צ'אט) באתר

באתר קיים צ'אט AI חי — כפתור צף "🤖 AI" בפינה שמאלית תחתונה בכל עמודי BTL — שמאפשר למבקרים לשאול שאלות על זכויות אזרחים ותיקים וביטוח לאומי ולקבל תשובה מבוססת על תוכן האתר בפועל (לא מודל כללי בלי הקשר).

**שינוי שם הקובץ ל-`btl-chat-claude-ai.js`, נוסף 28.07.2026 — הכנה למעבר אפשרי לג'מיני:** הפונקציה נקראה עד לתאריך זה `btl-chat.js`. שונה שם (`git mv` בלבד — **אין שום שינוי בלוגיקה/בקוד עצמו**, רק במיקום על הדיסק) כהכנה לארכיטקטורת "שני ספקים במקביל": בעתיד עשוי להיבנות `netlify/functions/btl-chat-gemini-ai.js` כמימוש מקביל ומלא מבוסס Google Gemini, שיתועד בפרק נפרד משלו. **עקרון מרכזי: שני המימושים (קלוד/ג'מיני) אמורים להתקיים זה לצד זה בקוד ב-git בו-זמנית**, ולא להחליף זה את זה - ההחלפה בפועל (איזה מהם "מחובר" לכפתור הצ'אט באתר) נעשית במקום אחד ויחיד: היעד של השורה `/api/btl-chat` בקובץ `_redirects` (ראו למטה). מעבר בין הספקים = שינוי שורה אחת שם + דיפלוי, בלי לגעת בקוד של אף אחד מהמימושים. חשוב: ה-widget בצד הלקוח (`btl-chat-widget.js`) **לא ספציפי לספק** ולא משתנה בכלל בין הספקים - הוא פונה תמיד לאותו `/api/btl-chat` בלי לדעת מי עונה בצד השני.

**חשוב — ארכיטקטורת "אינדקס + שליפה", לא דחיפת כל הקובץ:** תוכן כל עמודי המדריך המאוחד הוא מעל מגבלת ההקשר של המודל (200K) — **אסור לשלוח את כל התוכן ב-system prompt** (נוסה וגרם לשגיאת 400 "prompt is too long"). גם חיתוך שרירותי (נוסה קודם, 60000 תווים ראשונים) גרם לבאג אמיתי: המודל ראה רק את תחילת הקובץ וענה עם המספר הלא-נכון מקצבה אחרת (בלבל תקרת הכנסה של נכות כללית עם תקרת קצבת זקנה, כי רק הראשונה הייתה בטווח שנשלח). **הפתרון הנכון: אינדקס קצר + שליפה לפי דרישה**, מפורט למטה.

**רכיבים:**
- `netlify/functions/btl-chat-claude-ai.js` (בשורש הריפו, לא תחת `BTL/`) — Netlify Function שרצה בצד השרת. מקבלת שאלה מהדפדפן, קוראת **מהדיסק המקומי** (לא `fetch` חי לאתר) את כל קובצי `BTL/ai-content/*.html` (לפי רשימת `CONTENT_FILES` הקבועה בקוד — ראו הסעיף הקודם), מחלצת מכל אחד את הטקסט הגולמי מתוך ה-`<pre>` (`extractPreText()`), מאחדת אותם לטקסט אחד ומפרקת לסעיפים לפי המפריד `===== path =====` (ראו `build_ai_summary.py`), ובונה מהם **אינדקס קצר בלבד** (נתיב + תיאור קצר לכל עמוד) שנכנס ל-system prompt — לא את התוכן המלא. פונה למודל `claude-haiku-4-5-20251001` (נבחר לפי עלות — מתאים למשימת שאלות-תשובות מבוססת-טקסט כזו). **נוסה שדרוג ל-`claude-sonnet-5` ב-24.07.2026** בעקבות דפוס חוזר של תיקוני דיוק/הזיה נקודתיים בפרומפט - אבל **הוחזר ל-Haiku באותו יום** אחרי שהתברר בפועל שזמן התגובה של Sonnet (18-24+ שניות לשאלה, יותר בשאלות מרובות-סבבים) חורג ממגבלת ה-timeout של Netlify Function וגורם לכישלונות מוחלטים ("שגיאת תקשורת") אצל משתמשים אמיתיים - עדיפות אמינות על פני דיוק-מעט-גבוה-יותר עבור צ'אט ציבורי. אם ישוקל שדרוג עתידי, יש לבדוק קודם את תקרת ה-timeout בפועל של התוכנית (בדיקת curl ישירה מול ה-endpoint החי עם כמה שאלות מורכבות) ולשקול צמצום `MAX_TOOL_ROUNDS` לפני שינוי מודל. מקבלת רק בקשות שמקורן בדומיין `yairron.com` (בדיקת Origin/Referer). **שים לב:** הפונקציה לא קוראת בכלל את `BTL/ai-summary.html` (האינדקס הציבורי) — זה מיועד לכלי חוץ כמו ג'מיני, לא לפונקציה הפנימית, שבונה לעצמה אינדקס דומה משלה מתוך הסעיפים שהיא כבר פירקה.
- **קריאת `ai-content` מהדיסק, לא `fetch` (נוסף 25.07.2026):** עד תאריך זה הפונקציה עשתה `fetch` חי חזרה ל-7 קבצי `ai-content/*.html` ב-`https://yairron.com` בכל שאלה - למרות שהם כבר יושבים באותו deploy. זה latency מיותר (7 round-trips רשת בכל בקשה) שתרם לחריגות timeout שנצפו בפועל. הוחלף בקריאה סינכרונית מהדיסק (`fs.readFileSync`, קבועים `CONTENT_DIR`/`CONTENT_FILES` בתוך `btl-chat-claude-ai.js`). **דורש `included_files = ["BTL/ai-content/*.html"]` תחת `[functions]` ב-`netlify.toml`** - בלעדי זה הקבצים לא נכללים בפועל בחבילת הפונקציה ב-runtime של Netlify, למרות שהם קיימים ונקראים בהצלחה בהרצה מקומית.
- **`PATH_DESCRIPTIONS`** (אובייקט קבוע בתוך `btl-chat-claude-ai.js`) — תיאור קצר ואמין לכל עמוד באינדקס, מבוסס בפועל על ה-`meta description` האמיתי של אותו עמוד. **תחזוקה ידנית חובה:** ניסיון קודם לחלץ תיאור אוטומטית מהשורה הראשונה בכל סעיף נכשל בפועל (רוב השורות הראשונות הן ניווט כמו "חזרה לדף ראשי", לא תיאור נושא) — **כשמוסיפים עמוד חדש**, יש להוסיף גם שורה מתאימה כאן (בנוסף לשיבוץ שלו לנושא ב-`TOPICS`, ראו הסעיף הקודם), אחרת האינדקס נופל בשקט ל-fallback אוטומטי פחות מדויק (`previewOf`) שעלול לפגוע בדיוק בחירת העמוד.
- **כלי `get_page_content`:** המודל **חייב** להשתמש בו (לפי ההנחיה ב-system prompt) כדי לשלוף את התוכן המלא של עמוד רלוונטי מתוך הסעיפים שכבר פורקו מ-`ai-content/*.html` (לא שליפת HTML חי מהאתר, ולא צריך `stripHtml` יותר) — האינדקס לבדו לא מכיל מספרים/פרטים, רק כותרות. יכול לבקש כמה עמודים במקביל (מספר בלוקי `tool_use` בתגובה אחת) ואף כמה סבבים ברצף (עד `MAX_TOOL_ROUNDS = 3`, ואז הסבב האחרון נשלח בלי הכלי כדי לאלץ תשובה סופית ולא להיתקע ב"אחפש עוד" בלי לבצע בפועל).
- **קישורי "מקורות" בתשובה (`sources`, נוסף 22.07.2026):** בסוף כל בקשה, הפונקציה אוספת (ב-`usedPaths`, `Set`) את כל הנתיבים שבאמת נשלפו בהצלחה דרך `get_page_content` באותו סבב שיחה, ומחזירה אותם ב-JSON כשדה נפרד `sources: [{title, url}]` לצד `answer` — **לא** מבוססים על "ציטוט" שהמודל כותב בעצמו בטקסט (כדי לא להיות תלויים בזיכרון/דיוק שלו), אלא על הנתיבים בפועל שהתקבלו כקלט לכלי. הכתובת (`url`) נבנית כקישור לעמוד **עצמו** (לא לאקורדיון ספציפי בתוכו) לפי אותה נוסחת כתובת קנונית כמו ב-`build_sitemap.py` (`to_canonical_url`) - בלי סיומת `.html`. הווידג'ט (`btl-chat-widget.js`, `addMessage()`) מציג אותם כרשימת קישורים קטנה מתחת לתשובה.
- **`PATH_TITLES`** (אובייקט קבוע בתוך `btl-chat-claude-ai.js`, ליד `PATH_DESCRIPTIONS`) — הכותרת (כולל אימוג'י) שמוצגת כטקסט כל קישור מקור. **נבחרה במכוון להיות זהה לכותרת שמופיעה בקישור האמיתי לאותו עמוד במקום כלשהו באתר** (בעיקר האקורדיון "מדריכים מפורטים" ב-`BTL/index.html` והאקורדיונים ב-`additional_guides_index.html`) - לא כותרת חדשה שהומצאה, כדי שהמשתמש יזהה אותה. **תחזוקה ידנית חובה, באותו אופן כמו `PATH_DESCRIPTIONS`:** כשמוסיפים עמוד חדש יש להוסיף גם שורה תואמת כאן (ראו סעיף "כל עמוד חדש" למטה) - בלי זה הקישור יוצג עם נתיב הקובץ הגולמי בתור fallback, לא כותרת קריאה.
- `netlify.toml` (חדש, בשורש הריפו) — מצהיר רק על `netlify/functions` כתיקיית הפונקציות. לא נוגע בהגדרות build/publish קיימות של Netlify.
- `_redirects` (בשורש) — שורה נוספת שממפה `/api/btl-chat` ל-`/.netlify/functions/btl-chat-claude-ai`, מעל שורת ה-404 הקיימת. **זו נקודת ההחלפה בין ספקים** (ראו ההערה על שינוי השם למעלה, וגם הפרק הבא על מימוש הג'מיני) — כדי להחליף בין קלוד לג'מיני, משנים רק את היעד בשורה הזו. **מצב נוכחי (28.07.2026): השורה מצביעה בפועל על `btl-chat-gemini-ai`** — ג'מיני הוא הספק הפעיל כרגע באתר החי, לאחר בדיקה חיה מוצלחת (ראו הפרק הבא). לבדוק את התוכן העדכני של `_redirects` בפועל לפני הנחה כלשהי לגבי מי פעיל.
- `BTL/senior_rights/data/btl-chat-widget.js` — קובץ JS משותף **יחיד** שמכיל את כל ה-CSS/HTML/JS של הווידג'ט (כפתור + חלון צ'אט). כולל צבעים קבועים (לא `var(--primary)` וכו') כי לא לכל עמודי האתר יש אותם משתני CSS מוגדרים. **אסור להעתיק את קוד הווידג'ט בתוך עמוד — כל עמוד רק טוען את הקובץ המשותף בתג `<script>` אחד**, בדיוק כמו העיקרון הנהוג במנגנון האקורדיונים (סעיף "הוספת עמוד מדריך חדש" למטה). **לא ספציפי לספק** - עובד זהה מול קלוד וג'מיני, כי שניהם מחזירים אותו contract (`{answer, sources}`).
- **חותמת זמן בתשובות (נוסף 28.07.2026):** כל הודעת בוט (כולל הודעת הפתיחה) מקבלת תאריך+שעה קטנים ועדינים (`.btl-chat-timestamp`, לפי שעון הדפדפן של המשתמש) בתחתיתה, בפורמט `DD.MM.YYYY HH:MM` התואם למוסכמה בשאר האתר. נבנה ב-`addMessage()`/`formatTimestamp()` בתוך `btl-chat-widget.js` - גם זה לא תלוי-ספק.
- מפתח `ANTHROPIC_API_KEY` מוגדר כמשתנה סביבה ב-Netlify (Site settings → Environment variables, מסומן Secret), לא בקוד. **אין rate-limiting מובנה בתוך הפונקציה עצמה** — הוחלט במפורש לא להוסיף מנגנון כזה (כמו Netlify Blobs) כדי לא להכניס תלות npm/build חדשה לאתר שהוא כרגע סטטי לגמרי בלי שום build step. ההגנה מפני עלות בלתי מבוקרת היא spend limit שהוגדר ידנית בקונסולת Anthropic (Settings → Spend limits).
- **רישום שאלות (נוסף 25.07.2026):** `logQuestion()` בתוך `btl-chat-claude-ai.js` שולחת כל שאלה תקינה (אחרי ולידציה, לפני עיבוד) ל-Google Apps Script Web App חיצוני שכותב שורה לגיליון Google Sheet — כדי לדעת בפועל מה אנשים שואלים ולשפר את ה-FAQ בהתאם (בדיוק כמו התהליך שהוליד את שאלת "עולה חדש בן 68" ב-FAQ). **קוד הצד השני (Apps Script) נשמר לתיעוד בלבד ב-`netlify/chat-log-apps-script.gs.js`** — לא נפרס דרך git/Netlify, אלא מודבק ידנית בעורך ה-Apps Script המצורף לגיליון ומפורסם משם כ-Web App. דורש שני משתני סביבה נוספים ב-Netlify: `CHAT_LOG_WEBHOOK_URL` (כתובת ה-Web App) ו-`CHAT_LOG_SECRET` (טוקן משותף שנבדק גם בצד ה-Apps Script, הגנה נוספת מעבר לכך שכתובת ה-Web App עצמה כבר קשה לניחוש). **תכונה אופציונלית לגמרי** - אם משתני הסביבה לא מוגדרים, `logQuestion()` פשוט לא עושה כלום, בלי שגיאה. **נשמר בכוונה רק טקסט השאלה + חותמת זמן** - בלי IP, session, או כל מזהה אחר, כי השאלות כאן נוגעות למידע רגיש (בריאות/משפחה/הגירה/הכנסות). הקריאה **לעולם לא חוסמת/מפילה** את תשובת הצ'אט: `Promise.race` מול timeout של 3 שניות, וכל כישלון נבלע בשקט - נקראת מוקדם (בלי await מיידי) כדי לרוץ במקביל לקריאות ל-Claude שממילא לוקחות הרבה יותר זמן, וה-`await` בפועל קורה רק ממש לפני ההחזרה הסופית של תשובה מוצלחת. **חשוב - `Content-Type: text/plain;charset=utf-8` ולא `application/json` בכוונה:** אומת בפועל (25.07.2026) שכש-Content-Type הנכנס ל-Apps Script הוא `application/json`, עברית (וכל טקסט רב-בייטי) נהרסת ל-`?????` בגיליון - זה קורה גם עם `getDataAsString('UTF-8')` בצד הקורא (במקום `e.postData.contents` הרגיל, שגם הוא לא מספיק לבד). שני התיקונים ביחד (Content-Type בצד השולח + getDataAsString בצד הקורא, שניהם כבר מיושמים בקוד) נדרשים - זה באג מתועד של Apps Script בפענוח POST body רב-בייטי, לא טעות בקוד שלנו. הגוף עצמו נשאר JSON תקני; רק ה-header המוצהר משתנה כדי לעקוף את הבאג.

**נתיב הטעינה של `btl-chat-widget.js` בכל עמוד, לפי עומק התיקייה (זהה לעיקרון של נתיב `nii-constants.json`):**
- `BTL/index.html`: `senior_rights/data/btl-chat-widget.js`
- `BTL/senior_rights/*.html`: `data/btl-chat-widget.js`
- `BTL/new_immigrants/*.html`: `../senior_rights/data/btl-chat-widget.js`
- `BTL/additional_guides/html/*.html`: `../../senior_rights/data/btl-chat-widget.js`

**חריג במכוון:** `BTL/additional_guides/html/index.html` לא קיבל את הווידג'ט — זהו עמוד הפניה מיידית (`meta http-equiv="refresh"`) בלי תוכן, אין למשתמש זמן לראות אותו.

## מימוש הצ'אט מבוסס Gemini — `netlify/functions/btl-chat-gemini-ai.js`, נוסף 28.07.2026

מימוש מקביל ומלא ל-`btl-chat-claude-ai.js`, מבוסס Google Gemini במקום Anthropic Claude — אותה ארכיטקטורת "אינדקס+שליפה" בדיוק, אותו contract מול `btl-chat-widget.js` (`{question, history} → {answer, sources}`), **כפילות קוד מכוונת** מול הקובץ הקיים (לא חולץ מודול משותף) כדי לא לגעת בקובץ הקלוד העובד בשום צורה, גם לא ברפקטור "בטוח" לכאורה. הסיבה למעבר: תקציב ה-spend limit של Anthropic נגמר מהר מדי בפועל.

**מיפוי API עיקרי מול הגרסה של קלוד:**
- endpoint: `https://generativelanguage.googleapis.com/v1beta/models/{MODEL}:generateContent`, אימות בכותרת `x-goog-api-key` (לא `x-api-key`).
- `messages`/`role:'assistant'` של קלוד → `contents`/`role:'model'` של ג'מיני (לא `'assistant'`!). כל תוכן עטוף במערך `parts`.
- system prompt: לא בתוך `messages`/`contents` אלא בשדה נפרד `systemInstruction: {parts:[{text}]}`.
- כלי: `input_schema` של קלוד → `parameters` בתוך `functionDeclarations` בתוך `tools: [{functionDeclarations:[...]}]` של ג'מיני (עטיפה נוספת).
- אילוץ שימוש בכלי בסבב ראשון: `tool_choice:{type:'any'}` של קלוד → `toolConfig:{functionCallingConfig:{mode:'ANY'}}` של ג'מיני. **קריטי: מהסבב השני ואילך `mode` חייב לעבור ל-`'AUTO'`** (לא להישאר `'ANY'`) — אחרת המודל לעולם לא יכול לענות בטקסט, רק לקרוא לכלי שוב ושוב.
- זיהוי בקשת-כלי: קלוד מסמן מפורשות `stop_reason:'tool_use'`; ג'מיני **לא** מסמן סיבת עצירה ייעודית - הבדיקה היא אם יש חלק `functionCall` בתוך `candidates[0].content.parts`. ג'מיני תומך בכמה `functionCall` מקבילים באותו תור, בדיוק כמו קלוד.
- תגובת כלי חוזרת: קלוד רוצה בלוק `tool_result` עם `tool_use_id` תואם; ג'מיני רוצה `{functionResponse:{name, response:{...}}}` בתפקיד `'user'` - **בלי מזהה (`id`) בכלל**, ההתאמה היא לפי סדר/שם בלבד. ל-Gemini **אין** role `'function'` נפרד - Content.role הוא רק `'user'`/`'model'`.
- שימוש בטוקנים: `data.usage.{cache_creation_input_tokens,...}` של קלוד → `data.usageMetadata.{promptTokenCount,candidatesTokenCount,totalTokenCount}` של ג'מיני.
- קודי סטטוס בני-ניסיון-חוזר: קלוד כולל `529` (ספציפי ל"עומס" אצל Anthropic) - ג'מיני **לא** מכיר את זה, רק `429/500/503`.
- **אין מקביל למומש ל-`cache_control` של קלוד** (שמוזיל קריאות חוזרות ב-90%) - ל-Gemini יש מנגנון context caching נפרד לגמרי (`cachedContent`, endpoint ייעודי ליצירת cache מראש) שלא מומש בשלב הזה - זו הוצאה נוספת מודעת, לא שכחה.

**נבדק לפני פריסה** (בלי מפתח API אמיתי): לולאת tool-calling מלאה עם `fetch` מדומה - אילוץ `mode:'ANY'` בסבב ראשון ומעבר ל-`'AUTO'` בהמשך, טיפול בכמה `functionCall` מקבילים בתור אחד, ועצירה נכונה אחרי `MAX_TOOL_ROUNDS=3` (בלי ללכת ללולאה אינסופית, גם מול מודל "עקשן" שממשיך לבקש כלים).

**ממצאים מבדיקה חיה בפועל (28.07.2026), שלושה סבבים:**
1. **`429 RESOURCE_EXHAUSTED` — "Your prepayment credits are depleted":** התברר שהפרויקט ב-Google Cloud שנוצר עבור מפתח ה-API **לא** רץ על ה-Free Tier הטהור (מוגבל-קצב, אפס עלות) אלא על "Tier 1 · Prepay" - קורה אוטומטית ברגע שיש חשבון חיוב כלשהו מקושר לפרויקט (גם אם מסיבה לא-קשורה לג'מיני). הפתרון בפועל: הוספת יתרה קטנה (prepay) לאותו פרויקט - הרבה יותר זול מקלוד עדיין (`gemini-3.5-flash-lite`: $0.30/$2.50 למיליון טוקנים במחיר מלא, מול Haiku ב-$1.00/$5.00).
2. **`404 NOT_FOUND` — "model gemini-2.5-flash-lite is no longer available to new users":** הבחירה המקורית (`gemini-2.5-flash-lite`) הפסיקה להיות זמינה לפרויקטים/משתמשים חדשים. **תוקן ל-`gemini-3.5-flash-lite`**, אומת מול ה-model picker האמיתי ב-AI Studio (לא ניחוש) - מתואר שם כ-"fastest, most cost-effective 3.5 model for high-throughput execution", אותו שיקול עלות שהוביל לבחירת Haiku בגרסת הקלוד. **לוודא שוב מול `aistudio.google.com` (תפריט בחירת מודל בפועל, לא רק תיעוד) אם מתגלה עוד שגיאת 404 דומה בעתיד** - שמות המודלים הזמינים ב-Gemini מתחלפים בתדירות גבוהה יותר משנראה מהתיעוד הכתוב.
3. **תשובה עם מספרים מדויקים, אומתה מול `nii-constants.json` בפועל:** שאלת בדיקה על תקרות הכנסה לקצבת אזרח ותיק חזרה עם מספרים (10,113 / 14,402 / 30,339 / 34,627 ₪ וכו') שנראו במבט ראשון גבוהים בצורה לא-סבירה - אך אומתו כמדויקים **לחלוטין** מול `nii-constants.json` (השוואה ראשונית שגויה בוצעה מול תקרות של קצבה אחרת - נכות כללית - טעות בדיוק מהסוג שה-`SYSTEM_PREFIX` מזהיר מפניו). זה מוכיח בפועל ש-Gemini אכן משתמש בתוכן שנשלף דרך `get_page_content` ולא עונה מהזיכרון - הבדיקה החיה עברה בהצלחה.

**מה עוד לא נבדק/נבנה:** context caching (ראו למעלה), עמידות תחת עומס אמיתי, האם `SYSTEM_PREFIX` הקיים (שנוסח ונוסה במיוחד מול הגחמות הספציפיות של Haiku) דורש כוונון נוסף ספציפי לג'מיני לאורך זמן.

## תיקיית BTL/additional_guides — סיכומי PDF שהועברו מ-YR1

תיקייה זו מכילה 10 עמודי סיכום (`amnot_binleumiot.html`, `chovaat_hitatzbut.html`, `gamlay_zikna.html`, `hagdarat_tluim.html`, `mekarim_meyuchadim.html`, `nechut_mul_shairim.html`, `shaagat_haari.html`, `takrut_hachnasa.html`, `tkufat_achshara.html`, `yetzia_lachul.html`) שהועברו מהתיקייה הפרטית `YR1/BTL` לפרסום הציבורי, בתוספת עמוד ניווט ל-PDF-ים (`additional_guides_index.html`, לשעבר `YR_MAIN.HTML`) וקובץ הפניה (`index.html`). כל 12 הקבצים כלולים כעת ב-`build_ai_summary.py` וב-`build_sitemap.py` (סעיף קודם), וקיבלו `meta description`.

**עודכן:** חמישה מהקבצים (`takrut_hachnasa.html`, `nechut_mul_shairim.html`, `gamlay_zikna.html`, `amnot_binleumiot.html`, `mekarim_meyuchadim.html`) קיבלו מנגנון JS לערכים חיים ותיוג מלא, לפי תבנית קבוצה ד׳ — ראו רשימתם בסעיף "עדכון קבוצה ד׳" למטה, שם יש להריץ עליהם את שני הכלים אחרי כל עדכון, בדיוק כמו 4 העמודים המקוריים.

**שני קבצים יוצאים מהכלל במכוון, ולא יקבלו מנגנון:**
- `shaagat_haari.html` — הסכומים בו החלטת ממשלה חד-פעמית, לא נגזרים מביטוח לאומי.
- `hagdarat_tluim.html` — האחוזים בו (12.5%/8%/10%/5%, תוספת תלויים) קבועים בחוק ולא נגזרים מ-`nii-constants.json` כלל (נבדק ואומת: לא תואם לאף חישוב אפשרי מהקבועים הקיימים). נשאר טקסט קבוע לחלוטין.

שאר 4 העמודים (`chovaat_hitatzbut.html`, `tkufat_achshara.html`, `yetzia_lachul.html`, וכן `additional_guides_index.html`/`index.html`) אינם מכילים נתונים כספיים הנגזרים מביטוח לאומי, ולכן אינם דורשים מנגנון.

**עודכן 22.07.2026:** נוספו 4 עמודים חדשים, תמלול מלא (לא רק PDF גולמי) של 4 הקבצים שהיו עד אז ללא HTML מקביל תחת `BTL/additional_guides/data/` — `zchuyot_achrei_ishpuz.html`, `yipuy_koach_mitmashech.html`, `nechut_klalit_mul_avoda.html`, `oved_zar_bituach_leumi.html`. נבנו בדיוק לפי תבנית `mekarim_meyuchadim.html` (עיצוב/CSS/מנגנון אקורדיונים/תפריט צד/כפתורי ניווט מועתקים במדויק, ראו הכלל בסעיף "הוספת עמוד מדריך חדש" למטה), **ללא** תיוג `data-nii-key`/`data-nii-derived` (טקסט קבוע לחלוטין, כמו רוב העמודים הקיימים בתיקייה זו) — לא אומת אם מספרים בתוכן (למשל תקרות/סכומים) נגזרים מ-`nii-constants.json`; אם רוצים תיוג חי לערך ספציפי יש לאמת קודם כנגד `nii-constants.json` ולתעד את הנוסחה, בדיוק כמו בכל עמוד קבוצה ד׳ אחר. `additional_guides_index.html` עודכן: כל אחד מ-4 האקורדיונים המתאימים (עם ה-PDF הגולמי ב-iframe) קיבל קישור טקסט נוסף (`.html-version-link`) לגרסת ה-HTML המתומללת, מעל ה-iframe. **חשוב — תפריט הצד (`side-menu`) מועתק בזהות ל-15 העמודים** (11 קיימים + 4 חדשים) — קובץ נפרד `ALL_PAGES` בכל סקריפט/תבנית שמייצר את הרשימה הזו; אם מתווסף/מוסר עמוד נוסף מהמשפחה הזו, יש לעדכן את הרשימה בכל 15 (או 16+) הקבצים, לא רק בקובץ החדש.

**עודכן 27.07.2026:** נוסף עמוד 16 למשפחה — `chanaya_shmura_ezrach_vatik.html` (חניה שמורה לאזרח ותיק ללא תעודת נכה), תמלול מלא של PDF שהמשתמש העלה ישירות ל-`BTL/data/` (**לא** ל-`BTL/additional_guides/data/` כמו שאר הקבצים במשפחה זו — ה-iframe באקורדיון שלו מצביע ל-`../../../BTL/data/<שם-הקובץ-מקודד>.pdf`, נתיב שונה משאר האקורדיונים, כי הקובץ נשאר במיקום שבו הועלה ולא הועבר). נבנה מהיסוד לפי אותה תבנית בדיוק (לא הועתק מ-`mekarim_meyuchadim.html` הפעם, אלא מ-`nechut_klalit_mul_avoda.html` שנבדק לעומק כדוגמה). **חריג מכוון בתפריט הצד של העמוד עצמו:** הפריט העצמי (`class="current"`) **ללא** תכונת `href` בכלל (לא רק ערך זהה לעצמו כמו בכל שאר העמודים) — לפי בקשה מפורשת, כדי שלא יהיה קישור-לעצמו לחיצ'ה. תפריט הצד עודכן בכל 15 העמודים האחרים + `additional_guides_index.html` כדי לכלול קישור (עם `href` תקין) לעמוד החדש. שובץ לנושא `btl-special-cases` בתוך `TOPICS` (ליד `mekarim_meyuchadim.html`, מבחינה תוכנית — מקרה קצה בזכויות, לא נכות/קצבה ישירה).

**סעיף חדש בתפריט ההמבורגר הראשי (`hamburger-nav.js`), נוסף 27.07.2026:** `📚 סיכומים ומסמכי מידע מפורט` — בין `🛠️ כלי עזר` לבין `📊 עדכוני ביטוח לאומי 2026` במערך `MENU`. מכיל רשימה שטוחה (לא מקוננת) של קישורים לכל 15 האקורדיונים הראשיים ב-`additional_guides_index.html` (כל קבצי `additional_guides/html/*.html` פרט ל-`additional_guides_index.html` עצמו ול-`index.html` ההפניה), עם `bp + 'additional_guides/html/<שם-קובץ>.html'`. **אם מתווסף עמוד נוסף למשפחת `additional_guides`** — יש להוסיף שורה תואמת גם כאן (בנוסף לכל שאר הצעדים בסעיף הזה: תפריט הצד בכל הקבצים, `additional_guides_index.html`, וכל שלבי מאגר ה-AI למטה), אחרת העמוד החדש לא יהיה נגיש מהתפריט הראשי של שאר האתר (רק מתוך `additional_guides_index.html` עצמו).

**עודכן 27.07.2026 (בהמשך אותו יום):** נוסף עמוד 17 למשפחה — `dmei_michya_leshairim.html` (דמי מחיה לשאירים בביטוח לאומי: תוספת דמי מחיה ליתומים לומדים, ודמי מחיה לאלמן/אלמנה בשיקום מקצועי, כולל הנחיות מלאות למילוי טופס 2910), תמלול מלא של PDF שהועלה ישירות ל-`BTL/data/` (כמו `chanaya_shmura_ezrach_vatik.html` — לא ל-`BTL/additional_guides/data/`). האקורדיון שלו הוצב מתחת לאקורדיון `hagdarat_tluim.html` (לא בסוף הרשימה) — לפי בקשה מפורשת, כי הנושא קרוב תוכנית (שניהם עוסקים בתלויים/שאירים). **הדגמה מפורשת של אקורדיונים מקוננים בשני מפלסים (לא רק מפלס אחד כמו בכל שאר עמודי המשפחה עד כה):** בתוך אקורדיון 5 ("הנחיות למילוי טופס 2910"), תת-אקורדיון יחיד `data-level="4"` ("מפתח השלבים למילוי סעיפי הטופס") עוטף בתוכו 5 תת-תת-אקורדיונים `data-level="5"` (חלק א׳ עד חלק ה׳ של הטופס) — ממחיש שהמנגנון (`toggleAcc`/מאזין הקליק הגלובלי ל-`.sub-accordion-header`) תומך בעומק שרירותי מהיסוד, לא רק במפלס אחד; ה-CSS כבר הבחין מראש בין `data-level="4"` (כחול בהיר) ל-`data-level="5"` (ירוק בהיר) בדיוק לשימוש כזה. שובץ לנושא `btl-survivors-disability` בתוך `TOPICS` (ליד `hagdarat_tluim.html`/`nechut_klalit_mul_avoda.html` — נושא שאירים/תלויים). תפריט הצד עודכן בכל 16 העמודים האחרים + `additional_guides_index.html`, ותפריט ההמבורגר הראשי עודכן גם הוא (סעיף הקודם).

## עדכון "מנוע הליבה" — עמודים שתוכנם הוטמע מריצת דפדפן אמיתי

```
python BTL/support_files/senior_rights/scripts/bake_static_content.py <page.html>
```

הסקריפט טוען את העמוד בדפדפן אמיתי מול הערכים החדשים, לוכד את התוכן הסופי, מטמיע אותו בקובץ, ומריץ אוטומטית את `verify_render_parity.py` כדי לוודא שלא נוצר שינוי בלתי-רצוי.

**7 עמודים ידועים עם תוכן מוטמע בשיטה הזו — יש להריץ על כולם אחרי כל עדכון:**
```
nechut_vs_shairim.html
senior_citizens_rights_2026.html
nursing_home_guide.html
senior_rights_full.html
financial-tables-and-definitions.html
new_immigrants/gimlat_zikna_meyuchedet.html
new_immigrants/international_treaties.html
```

## עדכון קבוצה ד׳ — עמודים סטטיים עם ערכים חיים מסומנים בתגים

עמודים אלה **כבר** מכילים את כל התוכן שלהם כטקסט קבוע (לא תיבת טעינה) — רק ערכים בודדים מסומנים בתגים ומתעדכנים חי בדפדפן. יש להם ארבע תבניות תיוג אפשריות, בלי מוסכמה אחידה באתר: `data-nii="KEY"` (עם או בלי `data-format="..."` נלווה), `data-nii-key="KEY" data-nii-format="..."`, `data-nii-calc="KEY"` (ערך מחושב), `data-nii-derived="KEY"` (ערך מחושב).

```
python BTL/support_files/senior_rights/scripts/sync_static_values.py <page.html>
```

טוען את העמוד בדפדפן אמיתי, נותן לקוד של העמוד עצמו לחשב את כל הערכים (כולל מחושבים), וכותב את הטקסט הסופי בחזרה כערך הקבוע החדש. לבדיקה בלבד בלי לכתוב: `--check-only`.

```
python BTL/support_files/senior_rights/scripts/check_nii_values_sync.py <page.html>
```

בדיקה **עצמאית** — מחשבת בעצמה, ישירות מ-`nii-constants.json`, מה הערך הצפוי, בלי להסתמך על קוד הסנכרון. חובה להריץ את שני הכלים (לא להסתפק ב-`--check-only` של הראשון כבדיקה).

**4 עמודים ידועים בקבוצה זו — יש להריץ את שני הכלים על כולם אחרי כל עדכון:**
```
imputed_income_guide.html
old_pension_income_test_full_guide.html
survivors_benefits_guide_2026.html
women_transition_benefit_guide.html
```

**5 עמודים נוספים תחת `BTL/additional_guides/html/` — אותה שיטה בדיוק, יש לציין את הנתיב המלא (לא רק שם הקובץ) בהרצה:**
```
additional_guides/html/amnot_binleumiot.html
additional_guides/html/gamlay_zikna.html
additional_guides/html/mekarim_meyuchadim.html
additional_guides/html/nechut_mul_shairim.html
additional_guides/html/takrut_hachnasa.html
```
המנוע ב-5 העמודים האלה בנוי לפי התבנית האחידה (`data-nii-key`/`data-nii-derived` + `data-nii-format="currency|percent|plain"`) מהיסוד - אין בהם שימוש בתבניות הישנות. נתיב `nii-constants.json` בהם הוא `../../senior_rights/data/nii-constants.json` (עומק תיקיות שונה מ-`senior_rights/*.html`).

**חשוב — אין מוסכמה כלל-אתרית לעיצוב ערכים מחושבים או ל-`data-format`.** כל קובץ עשוי לממש את זה אחרת בקוד ה-JS שלו (למשל: אחוז עם או בלי סימן `%` בתוך התג עצמו). לכן הנוסחאות וכללי העיצוב מתועדים בנפרד לכל קובץ בתוך `check_nii_values_sync.py` (ב-`FILE_SPECIFIC_FORMAT_RULES` ו-`FILE_SPECIFIC_CALC_RULES`/`FILE_SPECIFIC_DERIVED_RULES`), ולא כלל גורף. **אם מוסיפים תג `data-nii-calc`/`data-nii-derived` חדש, או `data-format` חדש, בעמוד קיים או חדש — יש לקרוא את קוד ה-JS בפועל של אותו עמוד ולהוסיף כלל חדש שם, לא להניח שכלל מקובץ אחר תקף.** בלי כלל מתועד, הכלי מדווח "לא ניתן לאמת" — לא מאשר ולא נכשל בטעות.

**הערת אזהרה:** `benefit-combinations.html` מכיל מופעי `data-nii` בקוד המקור, אבל אלה חלק ממחרוזת טקסט בתוך פונקציה (`formatResult`) שרצה רק אחרי בחירת המשתמש בכלי — לא תוכן קבוע בעמוד. הוא **לא** שייך לקבוצה ד׳ ולא לרשימה למעלה; הוא כלי אינטראקטיבי (מחשבון), ואין צורך/טעם להריץ עליו את הכלים האלה.

## הוספת עמוד חדש מקבוצה ד׳ (תוכן סטטי עם ערכים חיים מסומנים)

חמשת העמודים הקיימים בקבוצה ד׳ משתמשים בארבע תבניות תיוג שונות, בלי מוסכמה אחידה — זה גרם לבלבול בפועל וכמעט גרם לפספוס שקט של ערכים. **כדי שזה לא יקרה שוב, כל עמוד חדש מהסוג הזה חייב להשתמש בתבנית האחידה הבאה, ולא באחת הישנות:**

- **ערך שנשלף ישירות מ-`nii-constants.json`:**
  ```
  <span data-nii-key="KEY" data-nii-format="currency|percent|plain">ערך</span>
  ```
- **ערך מחושב (נוסחה המשלבת כמה מפתחות, לא לוקאפ ישיר):**
  ```
  <span data-nii-derived="KEY" data-nii-format="currency|percent|plain">ערך</span>
  ```
- **משמעות `data-nii-format`, קבועה ואחידה (חובה לממש בקוד ה-JS של העמוד בדיוק כך, לא כל וריאציה אחרת):**
  - `currency` — הערך עם פסיקי אלפים ועם `₪` בתוך התג עצמו (למשל `1,838 ₪`).
  - `percent` — הערך עם סימן `%` בתוך התג עצמו (למשל `4.17%`).
  - `plain` — הערך הגולמי, בלי שום סימן נלווה.

**אסור** להשתמש בעמוד חדש בתבניות הישנות: `data-nii="KEY"` (בלי `data-nii-format`), `data-nii="KEY" data-format="..."` (עם מקף בודד), `data-nii-key`+`data-nii-format` בלי שהפורמט הוא אחד משלושת הערכים למעלה, או `data-nii-calc` (הוחלף לגמרי ב-`data-nii-derived` — גם לנוסחה פשוטה). התבניות הישנות עדיין נתמכות בכלים רק בשביל חמשת העמודים הקיימים, לתאימות לאחור בלבד.

**עבור ערך מחושב חדש** — יש להוסיף לו נוסחה מתועדת ב-`FILE_SPECIFIC_DERIVED_RULES` בתוך `check_nii_values_sync.py`, מפתח לפי נתיב הקובץ החדש, אחרי קריאה בפועל של קוד ה-JS של אותו עמוד (בדיוק כמו שנעשה עבור ארבעת העמודים הקיימים) — לא להניח שנוסחה מקובץ אחר תקפה.

## כל עמוד חדש, מכל סוג — חמישה דברים שאסור לשכוח

1. **תיאור קצר (`meta description`)** — חובה מהרגע הראשון. שורה אחת ברורה שמסבירה על מה העמוד, מיד אחרי תג ה-`viewport` ב-`<head>` (ראו כל עמוד קיים לדוגמה לפורמט).
2. **תג קנוני (`<link rel="canonical">`)** — להריץ מחדש את `add_canonical_links.py` (ראו הסעיף הקודם) כדי שהעמוד החדש יקבל תג המצביע על הכתובת הקנונית שלו. בלי זה, כתובת עם `.html` וכתובת בלי `.html` לאותו עמוד נשארות שתיהן חיות במקביל בלי הבחנה - זה בדיוק מה שגרם ל-Google Search Console לדווח "Duplicate without user-selected canonical" בפועל (27.07.2026).
3. **מפת האתר** — להריץ מחדש את `build_sitemap.py` (ראו הסעיף הקודם) כדי שהעמוד החדש ייכנס ל-`sitemap.xml`.
4. **קובצי הריכוז לבינה מלאכותית** — אם לעמוד יש תוכן קבוע אמיתי (לא מחשבון/טופס/תוכן תלוי-קלט), להוסיף אותו ל-`KNOWN_FILES` בתוך `build_ai_summary.py` עם `"include"` וסיבה קצרה, ואם לא — עם `"exclude"` וסיבה. בלי זה, `build_ai_summary.py` ידפיס עליו אזהרת "קובץ לא מסווג" בכל הרצה עד שיטופל. **אם הוספת ל-`"include"`** — שלושה צעדים נוספים, לא אחד: (א) לשבץ אותו לנושא קיים (או חדש) בתוך `TOPICS` באותו סקריפט, אחרת תודפס אזהרת "לא משובץ לנושא"; (ב) להוסיף שורה תואמת גם ל-`PATH_DESCRIPTIONS` וגם ל-`PATH_TITLES` בתוך `netlify/functions/btl-chat-claude-ai.js` (ראו סעיף "עוזר AI (צ'אט) באתר" למעלה) - הראשון כדי שבוט הצ'אט ידע מתי לבחור בעמוד החדש, השני כדי שקישור ה"מקורות" שמוצג למשתמש יקבל כותרת קריאה ולא נתיב קובץ גולמי; (ג) להריץ את `add_ai_alternate_links.py` (ראו הסעיף הקודם) כדי שהעמוד יקבל קישור `<link rel="alternate">` לקובץ הטקסט הנקי שלו, בשביל כלי AI חיצוניים כמו פרפלקסיטי/ג'מיני.
5. **כפתור צ'אט ה-AI** — להוסיף תג `<script src="...btl-chat-widget.js"></script>` מיד לפני `</body>`, עם הנתיב היחסי המתאים לעומק התיקייה (ראו טבלת הנתיבים בסעיף "עוזר AI (צ'אט) באתר" למעלה). לא להעתיק את קוד הווידג'ט עצמו — רק לטעון את הקובץ המשותף. חריג: עמודי הפניה מיידית בלי תוכן (כמו `additional_guides/html/index.html`) לא צריכים אותו.

**עמוד הפניה בלבד (redirect stub, כמו `additional_guides/html/index.html`) לא מקבל תג קנוני על עצמו** — הוא לא "עמוד" במובן הזה, הוא רק מפנה. `SKIP`/`REDIRECT_STUB_PAGES` בתוך `add_canonical_links.py`/`build_sitemap.py` כבר מחריגים אותו במפורש (גם מהתג הקנוני וגם ממפת האתר עצמה — הנחיית גוגל היא לא לכלול כתובות מפנות במפת אתר). אם נוסף עמוד הפניה נוסף בעתיד, יש להוסיף אותו לשתי הרשימות האלה.

## הוספת עמוד מדריך חדש (מבוסס ערכי ביטוח לאומי, מוצג באקורדיונים)

באתר קיימות כרגע שלוש צורות בנייה שונות לעמודי מדריך (התגלה בבדיקת מבנה מפורטת). כדי לא להוסיף צורה רביעית בכל פעם, יש לפעול לפי הכללים הבאים:

1. **ברירת המחדל היא תבנית הפיילוט, תמיד.** עמוד חדש נבנה לפי המבנה של `BTL/senior_rights/nechut_vs_shairim.html` / `BTL/senior_rights/senior_citizens_rights_2026.html`, אלא אם יש סיבה טכנית מפורשת שלא (ראו סעיף 5).

2. **מבנה קובץ הנתונים:** קובץ נפרד `BTL/senior_rights/data/<שם-עמוד>-data.js` בצורה:
   ```
   const X_DATA = { lastUpdate: "...", sections: [ { id, icon, title, contentFn: function(nii) { return `...`; } } ] };
   ```
   ערכי ביטוח לאומי נקראים רק דרך `nii[key].value` (עם עזרי `v`/`c`/`p` הרגילים) — אף פעם לא מספר קשיח בתוך התוכן.

3. **שכבת המנגנון (אקורדיונים) מועתקת, לא נכתבת מחדש.** את הפונקציות `displayContent()`, `initAccordions()`, `convertHeadingsToSubAccordions()`, `processNestedHeadings()`, `openFromHash()` יש להעתיק מילה-במילה מקובץ קיים מאותה משפחה (`nechut_vs_shairim.html`), ולא לכתוב גרסה חדשה. רק שם הקבוע, הכותרות, והנתיב היחסי ל-`nii-constants.json` (`data/...` תחת `senior_rights/`, `../senior_rights/data/...` תחת `new_immigrants/`) משתנים.

4. **שום עמוד לא "גמור" בלי הטמעה ובדיקה:**
   - להריץ `python BTL/support_files/senior_rights/scripts/bake_static_content.py <page.html>` כדי להטמיע תוכן סטטי אמיתי בתוך `#content` — אסור להשאיר קובץ עם תיבת טעינה בלבד. הסקריפט מריץ בעצמו בסיום גם את בדיקת הזהות (הסעיף הבא), ועוצר אם נמצא הבדל.
   - אם רוצים רק לבדוק בלי להטמיע: `python BTL/support_files/senior_rights/scripts/verify_render_parity.py <page.html>` — התאמה מלאה (או הפרש שמאומת כרעש רינדור גרידא) נדרשת לפני שהעבודה נחשבת גמורה.
   - אחרי כל עדכון עתידי של `nii-constants.json`, יש להריץ מחדש את `bake_static_content.py` לכל עמוד שכבר יש בו תוכן סטטי מוטמע (לא רק לעמוד החדש) — ראו רשימה בסעיף הקודם.

5. **אם באמת אין ברירה וצריך מבנה שונה** — קודם לבדוק אם זה מתאים לאחת משתי הצורות האחרות הקיימות (עץ מקונן ללא `contentFn` כמו `nursing_home_guide.html`, או קונפיג+פונקציות מוטמעות כמו `senior_rights_full.html`/`new_immigrants`). אם שום דפוס קיים לא מתאים — לעצור ולבקש אישור לפני יצירת מבנה רביעי, ואם אושר לתעד אותו כאן.

## חיפוש מבוסס-תוכן (search-index.json) — נוסף 28.07.2026

**הבעיה שהתגלתה בפועל:** תיבת החיפוש ב-`BTL/index.html` (`search-widget.js`/`search-extra.js`) מחפשת רק בטקסט של תוויות התפריט (`window.HNAV_MENU`) ובעוגנים שתויגו ידנית ב-`search-extra.js` — לא בתוכן העמודים עצמו. מילה אמיתית שמופיעה בתוך עמוד (למשל "תג" בתוך `chanaya_shmura_ezrach_vatik.html`, בתוך "תג נכה") לא עלתה בחיפוש בכלל, כי היא לא נמצאת בשום תווית קישור. הדיון המלא (כולל האפשרויות שנשקלו ונדחו) מתועד ב-`BTL/claude_last_chat.md`.

**הפתרון: אינדקס תוכן נפרד, ברמת אקורדיון/תת-אקורדיון (לא li/p), עם דה-דופליקציה בשרשרת הורים-ילדים.**

```
python BTL/support_files/senior_rights/scripts/build_search_index.py
```

בונה מחדש את `BTL/senior_rights/data/search-index.json` — לכל אקורדיון/תת-אקורדיון (בכל עומק) בכל עמודי המדריך (בכל שלוש משפחות התבנית, ראו הסעיף הקודם), ערך אחד: שרשרת הכותרות מהאקורדיון הראשי ועד אליו (גם גרסה מנורמלת `normPath` לצורך התאמה, וגם גרסה מקורית `rawPath` לתצוגה) + הטקסט המלא של הגוף. **הטקסט לכל ערך כולל בהכרח את התוכן של כל תתי-האקורדיונים המקוננים בתוכו** (כי אלה ילדים ממשיים ב-DOM) — כלומר אותה מילה שמופיעה בעומק תתאים גם לכל אקורדיון אב מעליה בשרשרת. זה מכוון, ולא באג — הדה-דופליקציה בפועל קורית בזמן חיפוש (ב-`search-widget.js`, ראו למטה), לא כאן.

**דה-דופליקציה בזמן חיפוש — `resolveMatchFrontier()` ב-`search-widget.js`, תוקן 28.07.2026 אחרי דיווח משתמש בפועל:** הגרסה הראשונה כיווצה כל שרשרת הורים-ילדים תואמת ל"האקורדיון הגבוה ביותר" בלי תנאי — וזה גרם לבאג אמיתי בעמוד `faq.html`: קטגוריה אחת ("תהליכים, זכויות וערעורים") מכילה כמה שאלות **נפרדות ולא-קשורות**, ושתיים מהן התאימו במקביל לאותו חיפוש ("תעודת נכה") — הכיווץ הפשטני הציג רק את הקטגוריה הרחבה, עם קטע-טקסט מתוך השאלה ה*לא*-רלוונטית, והסתיר לגמרי שאלה שנייה שכן ענתה במדויק. התיקון: מכווצים שרשרת יחידה (הורה עם בדיוק ילד תואם אחד, רקורסיבית) לאב הגבוה ביותר כמו קודם — אבל כשלאב יש **כמה** ילדים תואמים שונים (ענפים נפרדים, כמו כמה שאלות FAQ באותה קטגוריה), לא מכווצים לאב כלל: "מתפצלים" ומציגים כל ענף בנפרד (רקורסיבית, לפי אותו כלל). ההבחנה הזו (שרשרת יחידה = אותו תוכן שמורחב בעומק, מותר לכווץ; כמה ילדים תואמים = תכנים שונים לגמרי שבמקרה חולקים אב משותף, אסור לכווץ) היא הקריטית — לא "יש אב תואם" בלבד.

**קורא ישירות מ-`KNOWN_FILES` הקיים ב-`build_ai_summary.py`** (אותו מקור אמת יחיד לאילו עמודים הם תוכן מדריך אמיתי) — לא בונה רשימת סיווג מקבילה. **יש להריץ מחדש בכל פעם שמריצים `build_ai_summary.py`** (עמוד שנוסף/הוסר שם רלוונטי גם כאן), ובכל פעם שתוכן אקורדיון בעמוד קיים משתנה מהותית.

**קריטי — מקור אמת יחיד ל-JS, לא שכפול לפייתון:** לוגיקת החילוץ (`extractSearchEntries()`) ולוגיקת הנרמול (`normalizeTitle()`, מסירה אימוג'י/מספור מוביל מכותרות) חיות רק ב-`BTL/senior_rights/data/accordion-search-nav.js`. `build_search_index.py` **לא** מממש גרסה מקבילה בפייתון — הוא מזריק את הקובץ הזה בדיוק לתוך כל דף דרך Playwright (`page.add_script_tag`) ומריץ בתוכו את `extractSearchEntries()`. כך גם בניית האינדקס וגם הפתיחה בזמן אמת בדפדפן (`openFromPath()`, ראו למטה) תמיד משתמשות באותה נורמליזציה מילה-במילה — שכפול בשתי שפות היה מכניס סיכון שכל סטייה קטנה תגרום להתאמות ליפול בשקט.

**`openFromPath()` — ניווט "פתח לפי נתיב" בזמן אמת, גם ב-`accordion-search-nav.js`:** קורא פרמטר `?openPath=כותרת1|||כותרת2|||...` (מקודד, מנורמל), מוצא את האקורדיון הראשי שכותרתו תואמת לחלק הראשון, פותח אותו, ויורד בהתאמה בתוך תתי-האקורדיונים שלו לפי שאר החלקים — לפי **טקסט כותרת**, לא לפי `id` (ב-`openFromHash()` הקיים במשפחת הפיילוט/config+embedded), כי לרוב הכותרות באתר אין `id` יציב, ובוודאי לא במשפחת `additional_guides` שלא הייתה לה בכלל מנגנון עוגנים לפני זה.

**חובה לטעון את `accordion-search-nav.js` בכל עמוד שרוצים שתוצאת-תוכן תוביל אליו בפועל (לא רק תיאונדקס):**
- משפחת `additional_guides` (16 עמודים עם אקורדיונים): כבר טוענים אותו כברירת מחדל (ראו הסעיף הבא, `accordion-widget.js`) — פועל אוטומטית, כי התוכן שם סטטי וכבר קיים ב-DOM כשהתג נטען.
- משפחות הפיילוט / config+embedded: תוכן נבנה דינמית ב-JS, אז טעינת הקובץ לבד לא מספיקה — צריך גם קריאה מפורשת ל-`openFromPath();` מיד אחרי `openFromHash();` הקיים בקוד הבנייה של העמוד עצמו (בדיוק כמו שמוסיפים `window.addEventListener('hashchange', openFromHash)`). **נכון להיום מחוברים כך רק שני עמודי בדיקה, אחד לכל משפחה** — `senior_rights/nechut_vs_shairim.html` (פיילוט) ו-`senior_rights/senior_rights_full.html` (config+embedded) — כדי לאמת שהמנגנון עובד בכל משפחה לפני גלילה לכל שאר העמודים. `build_search_index.py` **כן** מאנדקס את כל העמודים בכל שלוש המשפחות ללא תלות בזה (מזריק את הקובץ בזמן בנייה בכל מקרה) — אבל תוצאת חיפוש שמצביעה על עמוד שעדיין לא חובר (כל שאר עמודי הפיילוט/config+embedded) תנווט לעמוד הנכון בלי לפתוח אוטומטית את האקורדיון המדויק. **כדי להרחיב תכונה זו לעמוד נוסף באחת משתי המשפחות** — להוסיף `<script src="data/accordion-search-nav.js"></script>` (נתיב יחסי בהתאם לעומק התיקייה, כמו `nii-constants.json`) ואת שורת הקריאה ל-`openFromPath()` ליד `openFromHash()` הקיים.

**`accordion-widget.js` — חולץ מ-16 עמודי `additional_guides` (28.07.2026):** עד לתאריך זה כל אחד מ-16 עמודי המשפחה הכיל עותק מודבק בנפרד (byte-identical) של `toggleAcc()` + מאזין קליק גלובלי לתתי-אקורדיונים + `openMenu()`/`closeMenu()` לתפריט הצד. חולץ לקובץ משותף יחיד, `BTL/senior_rights/data/accordion-widget.js` — כל 16 העמודים טוענים אותו עכשיו (`<script src="../../senior_rights/data/accordion-widget.js">`) במקום את הבלוק המודבק. **בכוונה לא נטען במשפחות הפיילוט/config+embedded** — הן כבר מצרפות מאזין קליק נפרד לכל כותרת תת-אקורדיון בעצמן (`initAccordions()`/`processNestedHeadings()`); טעינת מאזין-קליק גלובלי נוסף מ-`accordion-widget.js` שם הייתה גורמת לטריגר כפול (פתיחה-וסגירה מיידית באותה לחיצה). זה הסיכון שבגללו `accordion-search-nav.js` (הקובץ שכן משותף לכל שלוש המשפחות) **לא** נוגע בכלל בטיפול קליקים — רק בחילוץ אינדקס ובניווט `openFromPath()`.

**תוצאות חיפוש-תוכן ב-UI (`search-widget.js`):** נטען עצלנית — `search-index.json` (כ-1.6MB, יגדל עם הזמן) נשלף רק בהקלדה ראשונה בתיבת החיפוש, לא בטעינת העמוד, כדי לא להכביד על כל מי שלא משתמש בחיפוש. מוצג כקבוצת תוצאות נפרדת מתחת לתוצאות התפריט הרגילות ("נמצאו X תוצאות מתוך תוכן העמודים"), עם קטע טקסט (snippet) סביב ההתאמה בפועל ומודגש — לא רק כותרת+breadcrumb כמו תוצאות תפריט. דה-דופליקציה כפולה: (א) בתוך אותו עמוד, `resolveMatchFrontier()` (ראו למעלה); (ב) מול תוצאות התפריט הרגילות, לפי כתובת URL (לא מציג את אותו עמוד פעמיים).

## אפליקציית "מי נמצא" — YR1/whoIn

אפליקציית רשת נפרדת לגמרי מ-BTL, לקבוצת משתמשים סגורה (עד ~200 בשלב ראשון): כל משתמש יכול לפתוח "אירוע" ("מי נמצא?") ולהזמין אליו משתתפים ספציפיים ו/או קבוצות שלמות, והמוזמנים משיבים נמצא/נמצא חלקית/לא נמצא/לא יודע עדיין. הוראות הקמה מלאות (יצירת פרויקט Supabase, הרצת הסכימה, משתני סביבה ב-Netlify, בוטסטראפ אדמין ראשון) נמצאות ב-`YR1/whoIn/README.md` — **לא** כאן.

**ארכיטקטורה, בשונה מ-BTL:** זה עדיין אתר סטטי בלי build step (עקבי עם שאר הריפו), אבל עם משתמשים/נתונים דינמיים — לכן משתמש ב-**Supabase** (Postgres + Auth) כבסיס נתונים חיצוני, לא ב-Netlify Function כשכבת API. הלקוח (`assets/supabase-client.js`, טוען `@supabase/supabase-js` מ-CDN) מדבר **ישירות** מול Supabase; ההרשאות נאכפות ב-**Row Level Security** בבסיס הנתונים עצמו (`YR1/whoIn/sql/schema.sql`), לא בקוד. Netlify Function אחת בלבד קיימת — `netlify/functions/whoin-admin.js` — ורק לפעולות שחייבות Supabase Service Role Key (סוד): חסימה/ביטול חסימה/מחיקה של **משתמש** דרך Supabase Auth Admin API. מחיקת אירועים לא עוברת דרך פונקציה — זו מחיקת שורה רגילה מהלקוח, נאכפת ב-RLS (`is_admin()` או `creator_id`).

**מודל הרשאות מרכזי (ב-`schema.sql`):**
- אימות: אימייל כשם משתמש, מספר נייד כסיסמה ראשונית (הרשמה עצמית) — ראו הערת אבטחה ב-README על סיסמה חלשה, כוונה מפורשת לשלב ראשון בקבוצה סגורה.
- `profiles.is_admin`/`is_blocked` מוגנים בטריגר (`protect_privileged_columns()`) — משתמש רגיל לא יכול לשנות אותם לעצמו; רק `is_admin()` קיים או `service_role` (הפונקציה) יכולים.
- `public_profiles` (view עם `security_invoker = false` **בכוונה**) חושף רק `id`/`display_name`/`email` לכל משתמש מחובר, לבחירת משתתפים/חברי קבוצה — **אל תשנו ל-`security_invoker = true`**, זה ישבור את החיפוש.
- קבוצות: כל משתמש יכול ליצור קבוצה; **רק היוצר** מנהל את חבריה (מאושר במפורש, לא הצטרפות עצמית).
- אירוע גלוי/ניתן-למענה רק למי שיש לו שורה ב-`event_invitees` (מקור האמת להרשאות) — לא לכל 200 המשתמשים.
- ארכיון מחושב בשאילתה (`end_at + 24h < now()`), **אין** cron/Scheduled Function שמזיז שורות.
- כל בדיקת הרשאה שחוצה טבלה (למשל "האם אני מוזמן לאירוע X" בתוך policy על טבלה אחרת) עטופה בפונקציית `security definer` נפרדת (`is_event_creator()`, `is_event_invitee()`, `is_group_creator()`, `is_group_member()`, ליד `is_admin()`) — **לא** `exists(select ...)` גולמי בתוך ה-policy עצמה. זה הדפוס המתועד של Supabase למניעת הערכת RLS מקוננת בין שתי טבלאות שמפנות זו לזו (events↔event_invitees, groups↔group_members). אם מוסיפים policy חוצה-טבלה חדשה, יש להוסיף לה פונקציית עזר דומה, לא להעתיק את דפוס ה-exists הגולמי.

**PostgREST embedding מוגבל ל-FK אמיתיים:** `public_profiles` הוא view, לא טבלה עם FK — אי אפשר לעשות embed אוטומטי אליו (למשל `group_members.select('user_id, public_profiles(...)')` לא יעבוד). לכן `fetchProfilesByIds()`/`searchProfiles()` ב-`assets/supabase-client.js` עושות שליפה דו-שלבית (קודם המזהים, אחר כך `.in('id', ids)` מול `public_profiles`) בכל מקום שצריך שמות לפי רשימת מזהים. לעומת זאת ב-`admin.html` (שם הקורא הוא אדמין, ול-`profiles` המלאה יש FK אמיתי) יש שימוש ב-embed רגיל: `events.select('*, profiles(display_name)')`.

**כל דף** (`index.html`/`event.html`/`groups.html`/`group.html`/`archive.html`/`admin.html`) טוען את אותם שלושה קבצים משותפים — `assets/supabase-client.js` (אתחול + helpers: `requireAuth()`, `getMyProfile()`, `fetchProfilesByIds()`, `searchProfiles()`, `signOut()`) ואז `assets/header.js` (מזריק את הכותרת "מי נמצא" + ניווט משותף, כולל הצגה/הסתרה של קישור "מנהל מערכת" לפי `is_admin`) — **לא מעתיק קוד**, בדיוק לפי אותו עיקרון כמו `hamburger-nav.js`/`btl-chat-widget.js` ב-BTL. `login.html` הוא היוצא מן הכלל היחיד (כותרת סטטית בלי ניווט, כי עוד אין session).

**אם מוסיפים תג/עמודה/הרשאה חדשה כאן** — לעדכן גם את `sql/schema.sql` (טבלה/policy) וגם את `YR1/whoIn/README.md` אם זה נוגע להקמה; אין כאן סקריפט אוטומטי מקביל ל-`build_ai_summary.py`/`build_sitemap.py` של BTL — האפליקציה הזו לא מיועדת לאינדקס AI/מנועי חיפוש (`robots.txt` כבר חוסם את כל `/yr1/`), ואין לה עמודי תוכן קבועים.
